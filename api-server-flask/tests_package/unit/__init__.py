import pytest

# Optional: Setup code for the test package
print("Setting up unit tests package")

# Optional: Running the tests programmatically
# if __name__ == "__main__":
#     # This will automatically run all tests in the unit directory when this package is executed directly
#     pytest.main(["-v", "--tb=short", "tests_package/unit"])
